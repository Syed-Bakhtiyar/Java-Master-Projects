package interfaces_abstract_and_inner_classes.interfaces.isaveable;

import java.util.List;

public interface ISaveable {
    int a = 5;
    List<String> write();
    void read(List<String> savedValues);
}
